# deps
node_modules/

# secrets — NIKAD u repo (CLAUDE.md pravilo #4)
.env
.env.*
!.env.example

# build output
dist/
lib/
*.tsbuildinfo

# lokalni artefakti (mogu sadržati realne trening podatke)
summary.local.json
*.local.json
*.local.csv
*.local.md

# FIT-derived sesije (realni podaci — repo privatan, ali izvan git-a za svaki slučaj)
fixtures-fit/

# os / editor
.DS_Store
*.log

# firebase emulator secrets
.secret.local
functions/.secret.local
functions/deploy/

# expo prebuild (generisano — CNG workflow)
apps/mobile/ios/
apps/mobile/android/
apps/mobile/.expo/
