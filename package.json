{
  "name": "kaden",
  "private": true,
  "version": "0.0.0",
  "description": "Kaden - AI running coach, personal edition. Garmin (HealthKit/Health Connect + FIT) + Claude Sonnet.",
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "metrics:demo": "pnpm --filter @kaden/metrics demo"
  }
}
